# Overview

## Goal

Apply `ai-scripts-roadmap.md` by restructuring `.ai/` scripts and updating all hard-path references (docs + init pipeline) so the new entry points are discoverable and verifiable.

## Non-goals

- No business/app code changes
- No changes to skill pack contents (only controller placement/entry points)
- No DB state changes

## Status

done
