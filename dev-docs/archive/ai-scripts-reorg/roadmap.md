# Roadmap — `.ai/` scripts reorg (from `ai-scripts-roadmap.md`)

This task implements the agreed end-state described in `ai-scripts-roadmap.md`:

- Keep repo-level controllers in `.ai/scripts/`
- Move DB mirror tools into the database feature skill
- Move/rename the skill packs controller into `.ai/skills/_meta/`
- Merge skill deletion into `sync-skills.cjs` and remove `delete-skills.cjs`

Primary acceptance is captured in `04-verification.md`.

