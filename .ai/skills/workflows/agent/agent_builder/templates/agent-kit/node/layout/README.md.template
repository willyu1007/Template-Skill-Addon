# __AGENT_NAME__ (`__AGENT_ID__`)

__AGENT_SUMMARY__

## Quickstart

1. Set environment variables (see `.env.example`).
2. Start the API adapter:

```bash
node src/adapters/http/server.js
```

3. Health check:

```bash
curl -s http://localhost:${AGENT_PORT:-8080}__AGENT_BASE_PATH__/health
```

4. Run:

```bash
curl -s -X POST http://localhost:${AGENT_PORT:-8080}__AGENT_BASE_PATH__/run \
  -H 'content-type: application/json' \
  -d '{"input":{"text":"hello"}}'
```

## Entrypoints

- API: `src/adapters/http/server.js`
- Worker: `src/adapters/worker/worker.js` (if enabled)
- SDK: `src/adapters/sdk/index.js` (if enabled)
- Cron: `src/adapters/cron/run-cron.js` (if enabled)
- Pipeline: `src/adapters/pipeline/run-step.js` (if enabled)

## Notes

- This module is generated by `agent_builder`. It is designed to be **repo-integrated** and **maintainable**.
- Secrets must be provided via environment variables. Do not commit secrets.
